// DabbiExchange — CSV and JSON export and import.
//
// This target is part of the package skeleton (M0-02) so that the layering in Package.swift is fixed from the
// start. Its implementation arrives with work package M3-08; see docs/ARCHITECTURE.md §6.9.
