// DabbiQuery — predicates (`PredicateAST`), fetch templates, quick filter, global search, code generation.
//
// This target is part of the package skeleton (M0-02) so that the layering in Package.swift is fixed from the
// start. Its implementation arrives with work package M2-01; see docs/ARCHITECTURE.md §6.5.
