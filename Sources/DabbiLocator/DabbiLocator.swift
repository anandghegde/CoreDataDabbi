// DabbiLocator — finding stores: `StoreLocation`, the simulator index, SwiftData conventions, store search.
//
// This target is part of the package skeleton (M0-02) so that the layering in Package.swift is fixed from the
// start. Its implementation arrives with work package M1-09; see docs/ARCHITECTURE.md §6.7.
