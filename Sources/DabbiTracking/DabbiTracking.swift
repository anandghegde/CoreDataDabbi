// DabbiTracking — the change tracker: store watcher, scan and history strategies, `VersionLog`.
//
// This target is part of the package skeleton (M0-02) so that the layering in Package.swift is fixed from the
// start. Its implementation arrives with work package M2-06; see docs/ARCHITECTURE.md §6.6.
